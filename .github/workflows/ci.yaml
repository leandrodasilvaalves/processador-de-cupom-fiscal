name: CI/CD Pipeline

on:
  push:
    branches:
      - develop
      - 'release/**'
      - main
  pull_request:
    branches:
      - develop
      - 'release/**'
      - main

jobs:
  versioning:
    uses: laboratorio-net/actions/.github/workflows/git-version.yaml@v1
    outputs:
      version: ${{ jobs.versioning.outputs.version }}

  build_python:
    runs-on: ubuntu-latest
    needs: [versioning]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install webapi dependencies
        run: pip install -r src/webapi/requirements.txt
      - name: Install worker dependencies
        run: pip install -r src/worker_app/requirements.txt
      - name: Install test dependencies
        run: pip install pytest pytest-cov hypothesis opentelemetry-sdk
      - name: Lint with flake8
        run: flake8 src/ --max-line-length=120

  tests:
    runs-on: ubuntu-latest
    needs: [build_python]
    env:
      PYTHONPATH: src
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install webapi dependencies
        run: pip install -r src/webapi/requirements.txt
      - name: Install worker dependencies
        run: pip install -r src/worker_app/requirements.txt
      - name: Install test dependencies
        run: pip install pytest pytest-cov hypothesis opentelemetry-sdk
      - name: Run tests
        run: pytest src/tests/

  build_webapi:
    runs-on: ubuntu-latest
    needs: [tests]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build webapi Docker image
        run: docker build -f src/webapi/Dockerfile .

  build_worker:
    runs-on: ubuntu-latest
    needs: [tests]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build worker Docker image
        run: docker build -f src/worker_app/Dockerfile .

  publish_webapi:
    needs: [build_webapi, build_worker]
    uses: laboratorio-net/actions/.github/workflows/docker-build-publish.yaml@v1
    with:
      context: .
      dockerfile: src/webapi/Dockerfile
      username: ${{ secrets.DOCKERHUB_USERNAME }}
      image-name: ${{ secrets.DOCKERHUB_USERNAME }}/processador-cupom-webapi
      version: ${{ needs.versioning.outputs.version }}
    secrets:
      usertoken: ${{ secrets.DOCKERHUB_TOKEN }}

  publish_worker:
    needs: [build_webapi, build_worker]
    uses: laboratorio-net/actions/.github/workflows/docker-build-publish.yaml@v1
    with:
      context: .
      dockerfile: src/worker_app/Dockerfile
      username: ${{ secrets.DOCKERHUB_USERNAME }}
      image-name: ${{ secrets.DOCKERHUB_USERNAME }}/processador-cupom-worker
      version: ${{ needs.versioning.outputs.version }}
    secrets:
      usertoken: ${{ secrets.DOCKERHUB_TOKEN }}

  create_tag:
    needs: [publish_webapi, publish_worker]
    uses: laboratorio-net/actions/.github/workflows/create-tag.yaml@v1
    with:
      version: ${{ needs.versioning.outputs.version }}

  release_notes:
    runs-on: ubuntu-latest
    needs: [create_tag]
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Generate release notes
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.versioning.outputs.version }}
          generate_release_notes: true
          prerelease: ${{ github.ref != 'refs/heads/main' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
